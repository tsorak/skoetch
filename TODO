SOCKET
    Client socket isn't starting [window.onload]

CHAT
    Limit chat height
    Add autoscroll

CANVAS
    Add a canvas for each connected socket? (overlay them) 
    || have all sockets draw on the same canvas? (errors when multiple sockets are drawing at the same time?)

    Canvas prop for lines

    POINTER
        Get pointer position over the canvas

        ?
        onMouseDown
            
            if !wait
                wait = true
                getPointerPosition
                timeout x: ms --> wait = false


Client
    on initial connect
        populate chat 
        populate canvas
        


80 roomCode.tsx
42 roomHandler.ts

Går vi endast igenom javascript eller pratar vi även om Typescript med tanke på typer 